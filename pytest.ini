[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: fast, pure python
    component: single component with stubs
    integration: aws resources (moto/localstack)
    e2e: end-to-end
    security: security-as-code checks
    perf: performance tests
    prod_safe: safe to run on production
    slow: long running tests
